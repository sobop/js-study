// export default function () {  // default 는 함수 이름이 필요 없음
//     return Math.floor(Math.random() * 10)
// }
export function random() {
    return Math.floor(Math.random() * 10)
}
// default가 아니라 여러개 사용가능
export const user = {
    name: 'YS',
    age: 25
}
export default 123;
